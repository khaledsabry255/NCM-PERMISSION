# NCM Permission — بحث التصاريح

تطبيق بحث تصاريح الأفراد والمركبات لشركة NCM. البيانات كلها بتيجي من Google Sheet واحد.

> ⚠️ متخلطش بين المشروع ده ومشروع **NCM Permits** التاني (اللي بيقرا من Supabase وفيه صور موظفين). الاتنين اسمهم قريب بس دول ريبوهات وداتا مختلفة تماماً.

---

## مكوّنات المشروع

| المكوّن | المكان | الحالة |
|---|---|---|
| PWA (نسخة الويب) | جذر الريبو (`index.html`) | شغّالة إنتاج |
| تطبيق Android أصلي | `android/` | تحت التطوير |
| بناء الـ APK | `.github/workflows/` | GitHub Actions |

**الاستضافة:** GitHub Pages من جذر الريبو → https://khaledsabry255.github.io/NCM-PERMISSION/

> 🚨 **`index.html` لازم يفضل في جذر الريبو.** GitHub Pages بتدوّر عليه بالاسم ده بالظبط في الجذر. نقله لأي فولدر تاني = الموقع كله يرجع 404. (حصل قبل كده لما الملف كان اسمه `index_1.html`.)

---

## مصدر البيانات

Google Sheet: `18EH8aogUF0qZeLFjku7uiF3e9a_TjCTojRd53qMx0a0`

| التاب | gid |
|---|---|
| تصاريح أفراد | `1465451083` |
| تصاريح مركبات | `383739446` |

القراءة بصيغة **gviz** حصراً:
```
https://docs.google.com/spreadsheets/d/<ID>/gviz/tq?tqx=out:csv&gid=<GID>
```

> 🚨 **متستخدمش لينكات "Publish to web" أبداً.** بترجع خطأ CORS من GitHub Pages وبتوقّف التطبيق تماماً. ده استهلك وقت طويل في التشخيص قبل كده. الشيت لازم يفضل مشارك بصلاحية "Anyone with the link – Viewer" عشان لينكات gviz تشتغل.

### ترتيب الأعمدة (ثابت — بيتقرا بالـ index مش بالعنوان)

**تصاريح أفراد:** `2` الاسم · `3` الوظيفة · `4` الرقم القومى · `5` العنوان · `6` ملاحظات · `7` المرسل · `8` تصريح · `9` Mali Day

**تصاريح مركبات:** `2` رقم السيارة · `3` النوع · `4` تابعه · `5` تصريح · `6` Mail Day

> 🚨 أي تغيير في ترتيب أعمدة الشيت هيكسّر التطبيق من غير أي رسالة خطأ واضحة.

### منطق البيانات

- **تجميع التكرارات:** الصفوف بتتجمّع بالرقم القومى (أفراد) أو رقم اللوحة (مركبات). كل تكرار = "مرة" ليها تاريخ إرسال، مرتّبة زمنياً تصاعدي، والافتراضي المعروض هو آخر واحدة.
- **الصفوف الفاصلة** (الخضرا في الشيت) بتتعدّى تلقائياً لأن مفيهاش اسم أو رقم.

### تصنيف حالة التصريح

| القيمة في الشيت | اللون | العرض |
|---|---|---|
| فاضية | أصفر | في انتظار الرد / Awaiting response |
| `؟` أو `??` بس | رمادي | غير محدد موقفه / Status not yet determined |
| فيها "منع" | أحمر غامق | تتعرض زي ما هي / Banned |
| تجديد / رخصة منتهية / غيرت لوحات | وردي | تتعرض زي ما هي |
| "مؤقت" | رمادي | تتعرض زي ما هي / Temporary |
| تاريخ `dd-mm-yyyy` | أخضر (>30 يوم) · أصفر (≤30 يوم) · وردي (عدّى) | التاريخ |

التواريخ بصيغة `dd-mm-yyyy` أو `dd/mm/yyyy` — **مش ISO**.

---

## قفل الدخول

شاشة PIN من 4 أرقام (`1804` حالياً)، الإدخال من كيبورد الموبايل بس — **متضيفش لوحة أرقام داخل التطبيق**، بتتعارض مع كيبورد الموبايل.

**سحب الصلاحية من كل الأجهزة:** الجهاز بيخزّن رقم `ACCESS_VERSION` في `localStorage["ncm_device_unlocked"]`. تزويد الرقم في `index.html` = كل الأجهزة المفتوحة بتتقفل وتطلب الـ PIN من أول وجديد.

> 🚨 مع أي رفع، لازم تزوّد `CACHE_NAME` في `service-worker.js` كمان، وإلا الأجهزة تفضل شغّالة على النسخة المحفوظة في الكاش ومتشوفش التعديل.

> ⚠️ **ثغرة معروفة ومقبولة من المستخدم:** الشيت مشارك للعامة والـ PIN مكتوب في الكود، فأي حد يفتح سورس الصفحة يقدر يجيب كل الأسماء والأرقام القومية من غير ما يعدّي على شاشة القفل. المستخدم اتشرحله ده صراحةً في 17 أغسطس 2026 واختار يأجّله. الحل المطروح: Google Apps Script كـ backend يتحقق من الـ PIN، والشيت يرجع خاص.

---

## اللغة

زر AR/EN في الهيدر وفي شاشة القفل. بيقلب `lang`/`dir` (RTL ↔ LTR) ويترجم الواجهة + حالات التصريح. الاختيار محفوظ في `localStorage["ncm_lang"]`.

أسماء الأشخاص والعناوين وأنواع العربيات **بتفضل عربي زي ما هي في الشيت** — الترجمة للواجهة والحالات بس.

CSS لازم يستخدم خصائص منطقية (`inset-inline-start` / `padding-inline-*` / `text-align:end`) عشان الاتجاه يتقلب لوحده.

---

## الأمان والخصوصية

الداتا فيها **أسماء وأرقام قومية حقيقية**. متحطش أي داتا حقيقية في ملفات المشروع، ولا في رسائل commit، ولا في issues، ولا في أي مكان عام.

---

## أسلوب الشغل مع خالد

- الشرح بالعربي المصري، وخطوات الواجهة الرسومية بالتفصيل خطوة خطوة.
- **ابعت ملفات كاملة، مش patches أو أجزاء.**
- قبل ما تبعت الكود النهائي، اعرض الشكل الأول واستنى الموافقة.
- الرفع على GitHub بيتم يدوياً (سحب وإفلات من المتصفح).
